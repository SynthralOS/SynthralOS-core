name: Deploy to Railway

on:
  # Disabled - using Railway's GitHub integration instead
  # Railway will auto-deploy on push to main via dashboard connection
  # push:
  #   branches:
  #     - main
  workflow_dispatch:  # Can still be triggered manually if needed

